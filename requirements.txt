# =============================================================================
# Math Reasoning Agent — Core Dependencies (Streamlit Cloud compatible)
# =============================================================================
# For RL training dependencies, see pyproject.toml [project.optional-dependencies] rl

streamlit>=1.35.0
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.30.0
datasets>=2.18.0
numpy>=1.24.0
